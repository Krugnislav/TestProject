@model TestProject.Models.User

@if (Model != null)
{
    <li>@Model.Email</li>
    <li>@Html.ActionLink("Выход", "Logout", "LoginAdmin")</li>
}
else
{
    <li>@Html.ActionLink("Вход", "Index", "LoginAdmin")</li>
}